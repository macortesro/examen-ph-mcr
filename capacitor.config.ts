import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'examen.ph.mcr',
  appName: 'examen-ph-mcr',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
